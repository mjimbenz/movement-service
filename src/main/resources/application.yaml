spring:
  application:
    name: movement-service

  config:
    import: "configserver:http://config-server:8888" #config-server

  cloud:
    config:
      uri: http://config-server:8888 #config-server
      fail-fast: true